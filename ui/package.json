{
  "name": "orchard-workspace-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 1420",
    "build": "tsc --noEmit && vite build",
    "smoke": "npm run build && node scripts/smoke.mjs",
    "test:browser": "npm run build && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "typescript": "5.8.3",
    "vite": "6.3.5"
  }
}
